import { Box, Text, VStack } from "@gluestack-ui/themed";

export function PlaceInfo() {
  // const {  } = usePlaceInfo();
  // const {
  //   count: atomCount,
  // } = usePlaceInfoAtom();

  return (
    <Box>
      <VStack>
        <Text textAlign="center">PlaceInfo Component</Text>
      </VStack>
    </Box>
  );
}
