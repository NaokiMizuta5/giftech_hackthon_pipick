import { Box, Text, VStack } from "@gluestack-ui/themed";

export function RoutePath() {
  // const {  } = useRoutePath();
  // const {
  //   count: atomCount,
  // } = useRoutePathAtom();

  return (
    <Box>
      <VStack>
        <Text textAlign="center">RoutePath Component</Text>
      </VStack>
    </Box>
  );
}
