import { Box, Text, VStack } from "@gluestack-ui/themed";

export function PlaceDetail() {
  // const {  } = usePlaceDetail();
  // const {
  //   count: atomCount,
  // } = usePlaceDetailAtom();

  return (
    <Box>
      <VStack>
        <Text textAlign="center">PlaceDetail Component</Text>
      </VStack>
    </Box>
  );
}
