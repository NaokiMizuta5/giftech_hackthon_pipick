import { Box, Text, VStack } from "@gluestack-ui/themed";

export function Character() {
  // const {  } = useCharacter();
  // const {
  //   count: atomCount,
  // } = useCharacterAtom();

  return (
    <Box>
      <VStack>
        <Text textAlign="center">Character Component</Text>
      </VStack>
    </Box>
  );
}
