import {
  Box,
  VStack,
  Text,
} from "@gluestack-ui/themed";
import { use{{pascalCase name}}Atom } from "../../atom";
import { use{{pascalCase name}} } from "../../hooks";

export function {{pascalCase name}}() {
  // const {  } = use{{pascalCase name}}();
  // const {
  //   count: atomCount,
  // } = use{{pascalCase name}}Atom();

  return (
    <Box>
      <VStack>
        <Text textAlign="center">{{pascalCase name}} Component</Text>
      </VStack>
    </Box>
  );
}
